{% set product = variant.product %}
<img src="{{ product.image ? image(ftp_path~product.image.path).cropResize(177, 162).jpeg() : 'http://placehold.it/175x160' }}" alt="{{ product.name }}" width="100%" />
