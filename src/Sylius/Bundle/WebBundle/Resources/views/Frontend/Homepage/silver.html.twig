{% extends 'SyliusWebBundle:Frontend:layout.html.twig' %}

{% block title %}{{ page.metaTitle }}{% endblock %}
{% block metaDescription %}{{ page.metaDescription }}{% endblock %}
{% block metaKeywords %}{{ page.metaKeywords }}{% endblock %}

{% block border %}
    <div class="borderHead"></div>
{% endblock %}

{% block content %}
    <!-- Catalog -->
    <div class="catalog">
        <div class="head">
            Серебро
        </div>
        {{ render(url('sylius_silver_main')) }}
    </div>

    {% if page.enable is defined %}
        {% if page.enable == 0 %}
            <div class="content">
                {{ render(url('sylius_page_show_content', {'id' : 'silverBlock'})) }}
            </div>
        {% endif %}
    {% endif %}

{% endblock %}